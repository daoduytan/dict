const color = {
  primary: 'red',
  secondary: 'pink',
  text: '#000',
  border: '#f1f1f1'
};

const font = {
  family: {
    primary: `'Open Sans', sans-serif`,
    secondary: `'Playfair Display', serif`
  }
};

const size = {
  space: 15
};

export default { color, font, size };
