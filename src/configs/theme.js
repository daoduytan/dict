const color = {
  primary: '#0E9869',
  secondary: '#FEC282',
  text: '#000',
  border: '#f1f1f1',
  error: '#d65050'
};

const font = {
  family: {
    primary: `'Open Sans', sans-serif`,
    secondary: `'Playfair Display', serif`
  }
};

const size = {
  space: 15,
  borderRadius: 5
};

export default { color, font, size };
